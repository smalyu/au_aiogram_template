[project]
name = "au_aiogram_template"
version = "0.1.0"
description = "Add your description here"
# Tooling note: this project is managed with uv.
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiogram>=3.26.0",
    "loguru>=0.7.3",
    "pymongo>=4.16.0",
    "python-dotenv>=1.2.2",
    "redis>=7.4.0",
    "taskiq>=0.12.1",
    "taskiq-redis>=1.2.2",
    "telegram-sender>=1.6.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
    "ruff>=0.15.8",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
